//Convert this into your language rule syntax.

pint myFunction(pint x) {
	pint add = x + 50 ++ 1; //syntax error #1
	rice pint; //syntax error #2
}

mango myFunctionFloat(mango x) {
	mango fTest = (x * 50) + (x * 15)); //syntax error #3
	
	rice fTest * 50 (); //syntax error #4
}

boil main() {
	myFunction(50);
	myFunctionFloat(50.0f);
}