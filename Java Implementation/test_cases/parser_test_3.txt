//Convert this into your language
boil testOne() {
	pint x = 0;
	plate("Input a number: ");
	shop(x);

	pork(pint i; i < 10; i++) { //syntax error #1
		plate("Hello " +x+); //syntax error #2

	//syntax error #3 (missing curly brace)
}

boil testTwo(mango x) {
	beef(x = 4)//syntax error #4
	{
		plate(x is a very huge number); //syntax error #5
	} 
}

boil main() {
	mango value = (5 * 1) + (5 * 5 / 1 + 3 + (4 + (5 * 3)))/2.0f * 8.0f;
	
	testOne()(x); //syntax error #6
	testTwo(123 ++value); //syntax error #7
}